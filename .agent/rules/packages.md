---
trigger: always_on
---

- If the user asks for a new feature, search for NPM packages that can do it already and ask if the user wants to install it. Do not implement it from scratch without asking.
- Always install new packages using pnpm.